# ========================================
# AWS S3 Configuration
# ========================================
# Required for uploading shapefiles to S3
# Get your credentials from AWS IAM Console

AWS_REGION=us-zone
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
S3_BUCKET_NAME=your-bucket-name

# ========================================
# Instructions
# ========================================
# 1. Copy this file to .env in the project root:
#    cp .env.example .env
#
# 2. Replace the placeholder values with your actual AWS credentials
#
# 3. NEVER commit .env to git (already in .gitignore)
#
# 4. To get AWS credentials:
#    - Go to AWS Console > IAM > Users
#    - Create a new user or select existing
#    - Create access key for CLI/SDK
#    - Copy Access Key ID and Secret Access Key
#
# 5. Required S3 permissions:
#    - s3:PutObject
#    - s3:GetObject
#    - s3:HeadObject
#    - s3:ListBucket
